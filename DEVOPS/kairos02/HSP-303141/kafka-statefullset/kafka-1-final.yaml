apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data0-kafka-1       # Nombre original crítico
  namespace: kafka
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: thin-csi  # Driver nuevo
