apiVersion: v1
kind: Pod
metadata:
  name: migracion-kraft-1-restore
  namespace: kafka
spec:
  containers:
  - name: migracion
    image: confluentinc/cp-server:7.6.0
    command: ["/bin/sh", "-c", "sleep infinity"]
    securityContext:
      runAsUser: 0
    volumeMounts:
    - name: disco-temp
      mountPath: /mnt/temp
      readOnly: true
    - name: disco-final
      mountPath: /mnt/final
  volumes:
  - name: disco-temp
    persistentVolumeClaim:
      claimName: data0-kraftcontroller-1-temp
  - name: disco-final
    persistentVolumeClaim:
      claimName: data0-kraftcontroller-1
