apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-sp-csi
  namespace: sesiones-clinicas
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: thin-csi
